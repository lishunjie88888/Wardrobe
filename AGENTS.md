# AGENTS.md

本文件适用于整个项目。所有开发者与自动化代理在修改代码、数据模型或文档前，都必须遵守以下规范。

## 项目定位

- 本项目是准备长期维护的原生 macOS 电子衣橱应用，不是一次性 Demo。
- 技术栈优先使用 Swift、SwiftUI 与 SwiftData；引入其他框架或依赖前，应确认其长期维护价值和必要性。
- 设计与实现应优先考虑可维护性、可测试性、清晰边界和向后兼容，而不是短期开发速度。

## 架构与代码组织

- UI、业务逻辑、数据访问、文件存储和 AI 服务应合理分层，并通过清晰接口协作。
- SwiftUI View 只负责界面展示、用户交互和状态绑定；不得堆积复杂业务流程、持久化逻辑或网络调用。
- 避免巨型 SwiftUI View。视图职责过多时，应拆分为有明确边界的子视图、状态对象或功能模块。
- 避免全局可变状态。共享状态必须具有明确所有权、生命周期和依赖注入方式。
- 不允许为了快速实现加入明显的临时 hack、重复实现或绕过现有抽象。
- 不要无故重写、重构或大范围修改已经稳定的模块。发现架构问题时，应先说明问题，再采用影响最小且长期合理的方案。

## 数据模型与迁移

- SwiftData 主要保存业务元数据、关系和受控文件引用，不保存原始图片或生成图片的大体积二进制数据。
- 所有数据模型变更都必须考虑未来的 Schema Migration，包括默认值、可选性、关系变化和旧数据兼容。
- 新增功能或字段时优先保证向后兼容，不得无计划地破坏已有数据或备份格式。
- 架构、Schema、存储格式或迁移策略发生变化时，必须同步更新相关文档。

## 图片与文件存储

- 原始图片、人物全身照、AI 生成图片及其他大文件保存在应用的 Application Support 目录中，与 SwiftData 元数据分离。
- SwiftData 中的图片引用必须使用稳定的相对路径、持久标识符，或由受控的 Storage Service 管理；不得依赖易变化的绝对路径。
- 图片的导入、读取、写入、移动、删除、备份和恢复应统一经过 Storage Service，确保路径校验、错误处理和数据一致性。

## AI 服务与敏感信息

- AI 虚拟试穿必须通过统一的 `VirtualTryOnProvider` 抽象实现。
- UI 和业务界面层不得直接调用 OpenAI 或其他 AI 服务，也不得依赖具体供应商的请求或响应模型。
- 供应商 SDK、鉴权、网络请求、响应解析和错误映射应封装在 Provider 实现或其基础设施层中。
- API Key、令牌及其他敏感信息必须使用 Keychain 保存；不得写入源码、SwiftData、UserDefaults、日志或版本库。

## 修改流程

- 修改代码前，先阅读相关 `docs`、执行清单、现有实现、测试和相邻模块，确认当前约束与既有设计。
- 修改范围应紧扣当前任务，避免顺带进行无关清理、大规模格式化或架构改写。
- 新增功能时优先复用并扩展现有稳定抽象；确需改变公共接口时，应评估调用方、持久化数据和向后兼容影响。
- 架构、数据模型、公开接口、存储规则或开发流程发生变化时，更新对应文档与执行清单。

## 验证要求

- 每个开发阶段完成后，运行与修改相关的测试，并执行当前环境能够运行的 build。
- 必须修复由本次修改引入的编译错误、警告升级问题和测试失败。
- 如果某项测试或 build 因环境限制无法运行，应明确记录未验证的命令、原因和潜在风险，不得声称已经通过。
- 不得通过删除测试、降低断言强度或绕过类型与错误检查来制造通过结果。

## 文件操作安全

- 禁止批量删除文件或目录，也不得使用递归删除命令。
- 如确需删除，只能一次删除一个经过确认的明确文件路径；若任务需要批量删除，应停止并请用户手动处理。
